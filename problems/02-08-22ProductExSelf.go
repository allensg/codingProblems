package problems

// You are given an array of integers. Return an array of the same size
// where the element at each index is the product of all the elements in the original array except for the element at that index.

// Input: [1, 2, 3, 4, 5]
// Output: [120, 60, 40, 30, 24]
func (h *Handler) ProductExSelf(input []int) (returnString string, answer []int) {

	return returnString, answer
}
